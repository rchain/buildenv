FROM rchain/buildenv:latest
ARG GIT_URL=https://github.com/rchain/rchain
ARG GIT_REV=dev
RUN git clone $GIT_URL tmpclone && git -C tmpclone checkout $GIT_REV
RUN cd tmpclone && sbt update && rm -rf tmpclone
